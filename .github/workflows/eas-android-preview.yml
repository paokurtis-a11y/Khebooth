name: KHE Booth Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - apps/mobile/.eas-build-trigger

permissions:
  contents: read

concurrency:
  group: khe-booth-android-preview
  cancel-in-progress: false

jobs:
  build-android-preview:
    name: Build Android preview APK
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.14.0
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Setup Expo and EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Link existing Expo project
        working-directory: apps/mobile
        run: eas init --account kurtis-hypnotic-event --non-interactive --force

      - name: Validate EAS project
        working-directory: apps/mobile
        run: eas project:info

      - name: Start Android preview APK build
        working-directory: apps/mobile
        run: eas build --platform android --profile preview --non-interactive --no-wait
